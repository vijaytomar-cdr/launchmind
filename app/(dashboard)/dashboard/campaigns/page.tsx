/**
 * @file app/(dashboard)/dashboard/campaigns/page.tsx
 * @description Campaigns placeholder — populated in Week 3 strategy generation.
 */

export default function CampaignsPage() {
  return (
    <div className="p-8">
      <h1 className="font-display font-bold mb-2" style={{ fontSize: 22, color: 'var(--ink)' }}>
        Campaigns
      </h1>
      <p style={{ fontSize: 13, color: 'var(--ink2)' }}>
        Your campaign drafts will appear here after generating a strategy.
      </p>
    </div>
  );
}
